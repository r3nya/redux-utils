import { expect } from 'chai'
import createAction from '../src/createAction'

/*describe('createAction', () => {
	it('should make string action', () => {
		let expected = (value) => {
						return {
							type: 'DELETE',
							data: {
								id: value
							}
						}
					}
		let created = createAction('DELETE', 'id')
		expect(expected).to.equal(created)
	})
})*/